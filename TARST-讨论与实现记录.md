# TARST — Take a rest sometimes

> 本文件整理自本次产品讨论，用于在下一次继续开发时快速恢复背景。

## 初始愿景

用户希望开发一个 Voice Agent，并认为语音交互会是未来产品转型和迭代的重要方向。初始技术链路设想为：

```text
Wake words → VAD → ASR → LLM → Agent
```

核心判断是：语音并不只是文字的输入渠道；语气、句式、说话模式、停顿、节奏和能量等非文本信号，也构成了理解一个人的重要部分。用户注意到 ICLR / EchoMind 相关研究与这一方向相近。

## 市场与产品判断

当前产品已经将实时双向语音、打断、电话接入、工具调用和企业工作流逐步商品化。OpenAI Realtime、ElevenLabs、Vapi、Retell 等解决的是语音 Agent 的基础交互与业务自动化；Hume EVI 将韵律、节奏与音色纳入实时理解。

因此，TARST 的差异化不应建立在基础语音链路上，而应建立在：

> 长期、谨慎且可被用户纠正的个人状态理解，以及适时而低干预的陪伴。

TARST 的名字为 **Take a rest sometimes**。它应遵循“帮助用户恢复自主性、节律和清醒感，而非最大化对话时长或依赖”的产品原则。

## 产品定位（当前版本）

TARST 不是一个全能语音助手，也不应定位成心理治疗产品。

它是一个面向情绪陪伴与自我觉察的语音空间：当用户需要被理解、整理或安静地说完一段话时，愿意打开它。

产品宣言草案：

> TARST 是一个会听见言外之意、记得你如何生活，并在你忘记休息时轻轻提醒你的语音伙伴。

角色原则：

> 不抢着解决你的人生；先陪你把它说清楚。  
> 不追求让你一直说话；在该休息时，帮你停下来。

## 初步模式

| 模式 | 用户期待 | TARST 行为 |
|---|---|---|
| 日记模式 | 我想留下今天 | 少打断，结束后生成一份由用户确认的日记 |
| 倾听模式 | 我只想把话说完 | 将沉默视为能力，只做短而恰当的回应 |
| 整理模式（后续） | 我脑子很乱 | 区分事实、担忧、感受、可控项与下一步 |
| 复原模式（后续） | 我现在有点撑不住 | 降低语言密度，帮助稳定和休息，不急于解决问题 |
| 回望模式（后续） | 我最近怎么了 | 只基于用户授权保存的内容，呈现主题与趋势，不做诊断 |

### 第一版完整闭环

```text
用户开始自由自述（5–10 分钟）
    ↓
TARST 以倾听为主，低侵入地回应
    ↓
结束时询问：要不要帮你留下一页？
    ↓
用户确认或修改后，保存私人日记
    ↓
用户主动进入「回望」时，才查看长期模式
```

表达与分析应分开：不要在用户的每句话后都分析或总结。

## 对话体验准则

值得尝试的低干预回应：

- “嗯，我在。你可以继续。”
- “你刚才说‘没事’时停了一下。我不想过度解读，但如果你愿意，我们可以慢一点。”
- “你是想让我陪你把它说完，还是想一起想办法？”
- “我先不急着总结。你觉得还有哪一部分没说出来？”

重点不是展示“我识别出你悲伤”，而是使用声学信号改善介入时机、回应长度和表达方式。

所有状态推断应当是可撤销、可纠正且有不确定性的假设。例如：

> “我不确定，但你刚才说到这个项目时停顿变多了。是这件事让你累，还是我理解错了？”

## 推荐技术架构

```text
Web / Mobile Client
  麦克风、播放、会话 UI、本地隐私控制
          ↓ WebRTC
Realtime Session Gateway
  VAD / 打断 / 流式 ASR / LLM / 流式 TTS
          ↓ 事件流
TARST State Engine
  当下对话状态、声学线索、介入策略
          ↓ 异步
Diary & Memory Service
  用户确认后保存日记、回望与长期模式
```

第一版建议使用可控的级联式方案，并将声学分析作为独立侧通道：

```text
实时对话：流式 ASR → LLM / Agent → 流式 TTS
声学侧通道：语速、能量、停顿、音高、重叠说话、韵律、非言语音
状态层：仅保存用户确认的长期记忆；原始音频默认短暂处理或端侧处理
策略层：决定是否说话、说多长、追问或沉默
```

实时语音层可优先采用 OpenAI Realtime，以获得原生实时会话和打断；TARST 自己应独立拥有声学状态与对话介入策略，以便未来接入 Hume 或自研能力。

## 已实现的本地 MVP

项目根目录下已有最小可运行原型：

- `public/index.html`：倾听 / 日记模式的页面结构。
- `public/app.js`：浏览器麦克风权限、实时音量反馈、会话状态和日记确认交互。
- `public/styles.css`：暖白纸张与安静房间方向的视觉样式。
- `server.mjs`：无依赖的本地静态服务和 `POST /api/session` 示例接口。
- `package.json`：运行脚本。

运行方式：

```bash
npm run dev
```

然后访问 `http://localhost:4173`。当前服务已按用户要求停止。

## UI 迭代记录

第一版因黑底、功能卡和过强的“科技感”不符合 TARST 的情绪价值而被否决。

当前方向为“午后纸张 / 安静房间”：暖白底、鼠尾草绿、低对比墨色、留白优先；模式退为轻量标签，主动作只保留“开始说话”，音频状态以呼吸形状表达，而不是仪表盘或发光 AI 球。

## 下一步建议

1. 写出倾听模式的逐句对话脚本，明确不同停顿、重复、情绪转折时的沉默、回应与追问。
2. 用 OpenAI Realtime 接通真实语音会话；保留现有客户端会话接口。
3. 定义会话事件协议和用户确认后的日记数据模型。
4. 先只实现日记模式与倾听模式，不扩展为全能助手。
5. 确立隐私与风险边界：音频生命周期、记忆写入的用户确认、危机情境转介策略。
